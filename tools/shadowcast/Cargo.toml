[package]
name = "shadowcast"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.21", features = ["derive"] }
contour_tracing = { version = "1.0.12", features = ["array"] }
image = "0.25.5"
svg = "0.18.0"

shdw = { version = "*", path = "../shdw/" }

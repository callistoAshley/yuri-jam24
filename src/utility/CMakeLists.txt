set(DIR src/utility)
set(SOURCES
    ${DIR}/graphics.c
    ${DIR}/linked_list.c
    ${DIR}/list.c
    ${DIR}/log.c
    ${DIR}/vec.c
    ${DIR}/hashmap.c
    ${DIR}/hashset.c
    ${DIR}/files.c
    ${DIR}/time.cpp
    ${SOURCES}
    PARENT_SCOPE
)
